[provider]
name = "node"

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install] 
cmds = [
    "npm install -g pnpm@latest",
    "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
    "pnpm prisma generate",
    "pnpm build"
]

[start]
cmd = "pnpm start"