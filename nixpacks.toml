# Nixpacks configuration for Samaria ERP (Next.js 14 + Prisma)
# https://nixpacks.com/docs/configuration/file

[phases.setup]
# Node.js with OpenSSL for Prisma
nixPkgs = ["...", "openssl"]

[phases.install]
# legacy-peer-deps needed for this dependency tree
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
# Prisma generate needs DATABASE_URL set (doesn't connect, just reads the schema)
cmds = [
  "npx prisma generate",
  "npm run build"
]

[phases.build.variables]
DATABASE_URL = "postgresql://dummy:dummy@localhost:5432/dummy"

[start]
<<<<<<< HEAD
cmd = "npx prisma generate && cp -r .next/static .next/standalone/.next/static && cp -r public .next/standalone/public && node .next/standalone/server.js"
=======
# Push schema to DB, seed initial data, copy static assets, then
cmd = "npx prisma db push --skip-generate && node prisma/seed.mjs && cp -r .next/static .next/standalone/.next/static && cp -r public .next/standalone/public && node .next/standalone/server.js"
>>>>>>> parent of c149b6a (feat: add transporter creation page and simplify build command in nixpacks.toml)

